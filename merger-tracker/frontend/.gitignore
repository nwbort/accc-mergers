# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Embeddings are copied from <repo-root>/data/ at dev/build time by
# vite.config.js — the canonical files live there, not here.
public/data/embeddings.json
public/data/embeddings.bin

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
