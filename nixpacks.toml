[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m venv --copies /opt/venv && . /opt/venv/bin/activate && pip install -r merger-tracker/backend/requirements.txt"]

[start]
cmd = "cd merger-tracker/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
